<h2 class="text-center">Si pozabil Geslo?</h2>

<div class="row" style="margin-top: 30px;">
  <div class="col-md-4">

  </div>
  <div class="col-md-4">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, html: { class:"form form-horizontal"}) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, class:"form-control" %>
      </div>

      <div class="text-center">
        <%= f.submit "Pošlji mi navodila za novo Geslo", class:"btn btn-default" %>
      </div>
    <% end %>
  </div>
  <div class="col-md-4">

  </div>
</div>
